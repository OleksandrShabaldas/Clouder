namespace Clouder.CloudFilter;

public sealed class CloudFilterRegistrar
{
    // CfApi integration — registers Clouder as a Windows Cloud Storage Provider.
    // This makes the pool folder appear in Explorer's sidebar alongside OneDrive/MEGA.
}
